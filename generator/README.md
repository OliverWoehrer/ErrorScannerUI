# Testing
This folder holds a random log generator for testing the application.

Build an run the Docker compose network to see the error scanner working with random generated log messages
```
./project_root/> docker compose up --build
```