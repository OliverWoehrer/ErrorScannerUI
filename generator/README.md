# Testing
This folder holds a random log generator for testing the application.

## Getting Started
Build an run the Docker compose network. It starts multiple instances of the log generator and one error scanner instance. The Docker images are build with the Dockerfiles in the folders respectively.
```
./generator/> docker compose up --build
```