// React Components:

// Material Components:

// Local Import:
import "./style.css"


function Feed({ children }) {
    return (
        <div className="feed-layout">
            {children}
        </div>
    );
}

export default Feed;